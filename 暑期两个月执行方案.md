# 多模态推荐解释忠实性：单人暑期执行方案

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: plan
- Origin Date: 2026-07-29
- Verification Status: UNVERIFIED（尚未运行实验）
- Version Label: summer_plan_v1
- 执行者：单人
- 本地资源：RTX 4050
- 云端资源：约 30–60 GPU 小时（L4 或相近规格）
- 当前基础：尚未完整复现推荐模型
- 项目周期：8 周，建议预留第 9 周作为缓冲

---

## 1. 一句话定位

本项目研究：

> 多模态推荐模型实际依赖的图片、文本和协同信号，与 LLM 自然语言解释声称使用的证据之间，是否存在稳定的模态归因错配；若存在，能否通过行为审计得到的证据约束解释生成，降低这种错配？

项目不把“反事实＋LLM”本身作为创新，而把以下组合视为论文故事：

1. 一个明确现象：模型—解释模态归因错配；
2. 一套可复现的行为审计协议；
3. 图文冲突条件下的系统性诊断；
4. 一个轻量、可拒答的证据约束生成基线。

---

## 2. 目标优先级

### L0：必须完成

- 跑通统一推荐框架；
- 复现 LightGCN 与一个多模态模型；
- 完成模态级干预；
- 得到模型模态依赖与解释模态归因的对照结果；
- 发布可复现代码、配置、日志和实验报告；
- 形成可以用于算法/推荐实习面试的完整项目。

### L1：论文最低闭环

- 错配现象在至少两个模型或两个数据切分上成立；
- 图文冲突会稳定放大错配，或揭示新的、可重复的规律；
- Evidence-Grounded 方法相对 Direct Prompt 有显著且有实际意义的改进；
- 完成主实验、消融、稳健性和误差案例分析；
- 写出一篇完整投稿稿件。

### L2：冲刺扩展

- 增加第二个多模态模型；
- 增加第二个数据类别；
- 从模态级干预扩展到属性级干预；
- 增加不确定性校准与拒答；
- 增加小规模人工评价。

L2 只有在第 5 周 Go Gate 通过后才能开始，不允许提前并行堆功能。

---

## 3. 研究问题与假设

### RQ1：是否存在模型—解释模态归因错配？

对用户—商品对 `(u, i)`，推荐模型对模态 `m` 的行为依赖记为：

`D_m(u,i) = normalized_drop(score/rank after intervening on modality m)`

解释对模态 `m` 的归因权重记为：

`E_m(u,i) = proportion or weighted count of explanation claims grounded in modality m`

核心比较为：

`MAA(u,i) = agreement(D(u,i), E(u,i))`

其中可同时报告：

- Spearman 相关；
- Jensen–Shannon divergence；
- Top-1 modality accuracy；
- explanation-supported precision。

**H1：** Direct Prompt 生成的解释与推荐模型实际模态依赖之间存在显著错配。

### RQ2：什么条件会放大错配？

重点只研究三个预注册切片：

1. 图文一致 vs 图文冲突；
2. 稀疏历史 vs 丰富历史；
3. 协同主导模型 vs 多模态模型。

**H2a：** 图文冲突会降低模型—解释模态归因一致性。  
**H2b：** 历史稀疏时，解释更容易依赖可语言化的商品内容。  
**H2c：** 当模型实际更依赖协同信号时，LLM 仍倾向生成内容属性理由。

### RQ3：证据约束生成能否修复错配？

**H3：** Evidence-Grounded Prompt 能提高模态归因一致性和证据精确率，同时不显著降低可读性。

### 明确不作的因果声明

本项目测量的是：

- intervention-based behavioral dependence；
- counterfactual sensitivity；
- attribution agreement。

除非反事实构造满足更强的识别条件，否则不把分数变化称为真实世界“因果效应”，也不声称恢复了模型全部内部推理过程。

---

## 4. 研究范围

### 第一阶段数据

优先使用 MMRec 已支持的数据格式与处理特征，先在一个规模适中的 Amazon 类别上跑通。类别优先级：

1. Clothing / Fashion 类；
2. Baby；
3. Sports。

选择标准：

- 图片与文本覆盖率高；
- 用户和商品数量可在 4050 上调试；
- 属性容易解释；
- 与 MMRec 公开配置兼容。

### 第二阶段数据

仅在第一阶段通过 Go Gate 后，迁移到 Amazon Reviews 2023 的一个类别，使用其：

- user-item interaction；
- title / description；
- raw image 或 image URL；
- timestamp。

不要一开始下载全量数据。先做流式统计，再固定：

- 5-core 或更严格的交互过滤；
- 最多约 20k–50k items；
- 最多约 50k users；
- 图片和文本均有效的商品子集。

必须记录过滤前后用户、商品、交互及模态覆盖率，避免数据筛选成为隐藏自由度。

### 推荐模型

**必须：**

- LightGCN：协同信号基线；
- BM3：多模态主模型。

**可选扩展：**

- FREEDOM：只有 BM3 全流程成功且剩余云 GPU ≥ 20 小时时加入。

统一优先使用 MMRec 框架，不单独拼接多个仓库，以减少数据切分和评估协议差异。

### 解释方法

必须比较：

1. Direct Prompt；
2. Content Prompt（图片 caption＋文本）；
3. Evidence-Grounded Prompt；
4. Evidence-Grounded with Abstention。

可选：

- Collaborative-Aware Prompt；
- FIRE-like attribution-to-LLM baseline。

### 不做的内容

- 不训练新的大语言模型；
- 不做全参数微调；
- MVP 不做生成式图片编辑；
- 不追求推荐准确率 SOTA；
- 不把 LLM-as-a-judge 作为忠实性主指标；
- 不在第一阶段做大规模用户实验；
- 不同时复现超过三个推荐模型。

---

## 5. 最小系统设计

### 模块 A：推荐基线

输入：

- 用户—商品隐式交互；
- 商品图像特征；
- 商品文本特征。

输出：

- Recall@10/20；
- NDCG@10/20；
- 每个 `(u,i)` 的原始推荐分数和排名；
- 固定测试样本列表。

### 模块 B：模态级行为审计

对固定测试样本分别构造：

- `I_img_off`：替换为训练分布内的均值图像特征或匹配商品特征；
- `I_txt_off`：替换为训练分布内的均值文本特征或匹配商品特征；
- `I_hist_drop`：删除最相似历史项或一组相关历史项；
- `I_joint`：图片与文本联合干预。

每种干预至少采用两种实现：

1. 分布内替换；
2. 掩码/均值特征。

若两种实现结论方向不一致，标记为不稳定，不进入强结论。

对 LightGCN：

- 图片、文本并非模型输入，不计算虚假的图片/文本依赖；
- 协同依赖通过历史交互删除、邻居/边删除测量；
- 它用于检验 LLM 是否会给纯协同模型编造内容理由。

### 模块 C：解释生成

每个解释请求采用结构化输入：

```json
{
  "user_history_summary": [],
  "item_text_evidence": [],
  "item_image_evidence": [],
  "behavioral_audit": {},
  "allowed_claims": [],
  "unsupported_claims": []
}
```

输出强制为 JSON：

```json
{
  "explanation": "...",
  "claims": [
    {
      "claim": "...",
      "modality": "image|text|history|collaborative|unknown",
      "evidence_id": "...",
      "confidence": 0.0
    }
  ],
  "abstain": false
}
```

结构化输出能避免后续完全依赖另一个 LLM 抽取解释证据。

### 模块 D：图文冲突测试

MVP 使用真实商品的跨样本匹配构造，不使用生成式图片编辑：

- 固定类别和大致风格；
- 替换图片或文本；
- 使目标属性/语义发生冲突；
- 控制流行度、交互量和特征距离。

构造三组：

1. 原始一致样本；
2. 随机替换负对照；
3. 匹配式冲突样本。

随机替换负对照用于判断结果是否只是任何扰动都会造成。

---

## 6. 样本与计算预算

### 开发样本

- 先固定 100 个用户—商品对；
- 用于调试干预、JSON 输出和指标；
- 不用于最终显著性结论。

### MVP 评估样本

- 至少 1,000 个 `(u,i)`；
- 建议 2,000 个；
- 按用户历史长度与商品热度分层抽样；
- 每个用户最多贡献固定数量样本，避免活跃用户支配结果。

### 完整评估样本

- 3,000–5,000 个 `(u,i)`；
- 只在调用成本、运行时间和结果稳定时扩展。

### 随机种子

- 开发阶段：1 个 seed；
- 主结果：至少 3 个 seed；
- 若训练方差大或核心差异接近零：扩展到 5 个 seed。

### GPU 分配建议

| 任务 | 设备 | 预算 |
|---|---|---:|
| 环境、单元测试、小切分 | RTX 4050 | 8–15 小时 |
| LightGCN 调参与复现 | RTX 4050 | 5–10 小时 |
| BM3 单 seed 完整训练 | L4 | 3–8 小时 |
| BM3 三 seeds＋必要调参 | L4 | 12–24 小时 |
| 特征编码/批量 caption | L4 | 8–15 小时 |
| FREEDOM 扩展 | L4 | 8–15 小时 |
| 预留失败与复跑 | L4 | 至少 20% |

禁止把云 GPU 用于：

- 安装环境；
- 下载数据；
- 调试路径；
- 小样本 JSON 解析；
- 无日志的盲目网格搜索。

### LLM/VLM 策略

- 解释模型先使用 7B–8B 级指令模型的量化推理，或固定 API 模型；
- 图像概念优先离线预提取并缓存；
- 所有 prompt、模型版本、解码参数必须写入日志；
- `temperature=0` 或尽可能低；
- 同一实验不得中途更换模型版本；
- API 结果不可视为严格可复现，必须保存原始响应。

---

## 7. 指标定义

### 7.1 推荐性能

- Recall@10、Recall@20；
- NDCG@10、NDCG@20；
- 可选 Precision@K。

推荐性能用于证明基线正常，不是本项目主要贡献。

### 7.2 行为依赖

每个模态报告：

- 原始分数变化；
- 相对分数变化；
- 排名变化；
- Top-K flip rate。

不能只用“置零后分数下降”作为唯一证据。

### 7.3 模态归因一致性

主指标：

- Top-1 Modality Agreement；
- Spearman correlation；
- Jensen–Shannon divergence。

辅助指标：

- cosine similarity；
- modality claim precision；
- unsupported modality claim rate。

### 7.4 证据忠实性

- Evidence Precision；
- Evidence Recall；
- Evidence F1；
- Necessity / deletion；
- Sufficiency / insertion；
- Counterfactual Explanation Consistency；
- Abstention precision、recall、coverage。

### 7.5 文本质量

只作为约束指标：

- 人工可读性；
- 相关性；
- 具体性；
- 重复率；
- 平均长度。

如使用 LLM-as-a-judge：

- 仅评价可读性等语言性质；
- 抽取 10%–20% 样本人工复核；
- 不让 judge 决定模型真实使用了什么证据。

---

## 8. 统计分析计划

### 分析单位

主分析单位为用户—商品对，但标准误需考虑同一用户内相关性：

- 优先按用户 cluster bootstrap；
- 或先按用户聚合后做配对检验。

### 主比较

Evidence-Grounded vs Direct Prompt：

- 配对 bootstrap 95% CI；
- Wilcoxon signed-rank test，适用于非正态配对差异；
- 同时报告均值/中位数差异与效应量。

### 多重比较

- RQ1、RQ2、RQ3 各自指定一个主指标；
- 其他指标作为辅助；
- 同一假设族使用 Benjamini–Hochberg FDR；
- 不把大量异质性切片中的偶然显著结果写成主要发现。

### 稳健性要求

强结论至少满足：

1. 三个随机种子方向一致；
2. 两种干预实现方向一致；
3. 95% CI 不跨越零，或效应大小达到预设的实际阈值；
4. 去除异常用户/热门商品后仍成立。

### 预设实际意义阈值

在 pilot 后、查看完整测试集前固定：

- Top-1 Modality Agreement 至少提升 5 个百分点；
- Unsupported Claim Rate 至少相对下降 20%；
- 可读性平均分下降不超过 0.3/5；
- 推荐性能不因解释模块改变，因为解释是 post-hoc；若改变则说明实现混淆。

---

## 9. 八周执行时间表

### 第 1 周：工程地基

目标：

- 建立 Git 仓库结构；
- 创建固定 Python 环境；
- 跑通 MMRec 示例；
- 理解数据切分、负采样和评估流程；
- 在小数据上跑通 LightGCN。

验收：

- 一条命令训练；
- 一条命令评估；
- 指标和日志自动保存；
- 固定 seed 可重复运行；
- 写出 `docs/reproduction_notes.md`。

停止条件：

- 第 7 天仍不能稳定跑通框架，则暂缓研究创新，专门用 3 天解决复现。

### 第 2 周：推荐基线

目标：

- LightGCN 完整训练；
- BM3 单 seed 跑通；
- 与公开仓库结果核对量级；
- 固定数据、切分和主配置。

验收：

- 两模型均产出 Recall/NDCG；
- 训练曲线无明显异常；
- 能对任意 `(u,i)` 输出分数；
- checkpoint 可恢复；
- 数据统计表完整。

Gate A：

- 若 BM3 无法复现，先用 VBPR 或更简单的多模态模型替代，不能在环境问题上耗尽整个暑假。

### 第 3 周：行为审计

目标：

- 完成图片、文本、历史三类模态级干预；
- 完成随机替换负对照；
- 记录 score/rank/top-K 变化；
- 对 100 个开发样本人工检查。

验收：

- 每种干预都有单元测试；
- 干预后张量维度和数值范围正常；
- 两种干预构造能比较；
- 输出统一 `audit_results.parquet`。

### 第 4 周：解释与证据解析

目标：

- 固定解释模型；
- 完成 Direct、Content、Evidence-Grounded、Abstention 四个版本；
- 强制结构化 claims；
- 完成解释—模态归因指标。

验收：

- JSON 合法率 ≥ 98%；
- 失败请求可重试并有记录；
- 不同方法使用相同输入样本；
- 保存所有 prompt 和原始输出。

### 第 5 周：Pilot 与第一次 Go/No-Go

目标：

- 在 1,000–2,000 个样本上回答 RQ1；
- 检查错配是否稳定；
- 检查简单 Prompt 是否已经完全解决；
- 形成第一张主结果表和两张图。

Go 条件，六项中至少满足四项，且第 1 项必须满足：

1. 至少一个清晰的错配现象，95% CI 稳定；
2. LightGCN 会被解释成内容理由；
3. BM3 的解释模态与行为依赖不一致；
4. 两种干预实现方向一致；
5. Evidence-Grounded 有 ≥ 5pp 的一致性提升；
6. 发现明确的用户/商品条件效应。

No-Go 条件：

- 换干预方式后结论反转；
- 结果完全由均值置换等分布外输入导致；
- 错配接近零；
- 一个简单提示就消除全部问题；
- 解释证据无法可靠结构化或人工验证；
- 只能得到“LLM 有时会幻觉”这种常识结论。

No-Go 后的转向：

- 保留工程项目；
- 把主题改为“多模态推荐模型的模态利用诊断与可视化”；
- 不继续投入属性级反事实和论文包装。

### 第 6 周：图文冲突与稳健性

目标：

- 构造匹配式图文冲突集；
- 对照随机替换；
- 分析冲突对模型和解释的不同影响；
- 做 cluster bootstrap 与 FDR。

验收：

- 冲突构造规则可复现；
- 有自动质量检查；
- 不只展示个例；
- 至少一个预注册 RQ2 分析完成。

### 第 7 周：方法完善与消融

目标：

- 完成 Evidence-Grounded 与 Abstention；
- 消融证据来源、阈值和拒答；
- 三 seeds 主实验；
- 记录成本和时延。

最小消融：

- 无图片审计；
- 无文本审计；
- 无历史审计；
- 无拒答；
- 单一干预构造；
- 不提供行为依赖强度。

### 第 8 周：交付

目标：

- 清理代码与 README；
- 固化环境；
- 生成全部表格和图；
- 写技术报告/论文初稿；
- 准备简历表述与面试演示。

必须交付：

- 可复现仓库；
- 数据处理说明；
- 一键主实验命令；
- 原始结果与聚合脚本；
- 6–8 页技术报告或论文草稿；
- 5 分钟演示；
- 1 页项目总结。

### 第 9 周：缓冲（如有）

- 失败复跑；
- 补三 seeds；
- 核对统计；
- 整理论文图表；
- 不新增大模块。

---

## 10. 代码仓库设计

```text
faithrec-mm/
├── README.md
├── pyproject.toml
├── environment.yml
├── configs/
│   ├── data/
│   ├── models/
│   ├── audits/
│   └── explanations/
├── data/
│   ├── README.md
│   ├── raw/                 # gitignore
│   ├── processed/           # gitignore
│   └── manifests/
├── src/
│   ├── data/
│   ├── models/
│   ├── interventions/
│   ├── explanations/
│   ├── metrics/
│   └── analysis/
├── scripts/
│   ├── prepare_data.py
│   ├── train.py
│   ├── audit.py
│   ├── generate_explanations.py
│   ├── evaluate.py
│   └── reproduce_main.sh
├── tests/
├── outputs/                 # gitignore，大文件
├── results/
│   ├── tables/
│   ├── figures/
│   └── manifests/
├── prompts/
├── docs/
│   ├── research_protocol.md
│   ├── reproduction_notes.md
│   └── failure_log.md
└── paper/
```

每次运行必须生成 manifest：

```json
{
  "git_commit": "...",
  "dataset_hash": "...",
  "config": "...",
  "seed": 2026,
  "hardware": "...",
  "start_time": "...",
  "end_time": "...",
  "output_paths": []
}
```

---

## 11. 实验矩阵

### MVP 主矩阵

| 维度 | 设置 |
|---|---|
| 数据 | 1 个 Amazon 类别 |
| 模型 | LightGCN、BM3 |
| 解释 | Direct、Content、Grounded、Grounded+Abstain |
| 干预 | image、text、history、joint |
| 干预实现 | matched replacement、mean/mask |
| 条件 | normal、image-text conflict |
| seeds | 3 |

这已经是 `2 × 4 × 4 × 2 × 2 × 3` 的组合。实际运行时应缓存推荐分数和解释，不能为每个指标重复推理。

### 扩展矩阵

只允许新增其中一个：

- 第二模型 FREEDOM；或
- 第二数据类别；或
- 属性级干预。

不得三者同时增加。

---

## 12. 质量控制

### 数据

- 固定用户/商品 ID 映射；
- 检查 train/validation/test 泄漏；
- 记录模态缺失率；
- 图片与商品 ID 对齐抽查；
- 冲突样本不得进入训练集；
- 测试集在 pilot 后冻结。

### 推荐模型

- 与公开配置对照；
- 记录 best epoch 和 early stopping；
- 不只保留最好 seed；
- 对异常指标检查负采样与全排序差异；
- 不直接比较采用不同切分的论文数字。

### 解释

- prompt 版本化；
- 保存原始输入输出；
- 固定解码参数；
- JSON 解析失败单独统计；
- 人工复核至少 100 条或 10% 的证据标签；
- 报告人工一致性，若只有本人标注则明确局限。

### 反事实

- 报告干预前后特征范数和相似度；
- 设置随机扰动负对照；
- 检查是否引入分布外值；
- 同时观察 score 和 rank；
- 不把模态删除等同于属性语义贡献。

---

## 13. 风险登记表

| 风险 | 概率 | 影响 | 触发信号 | 应对 |
|---|---:|---:|---|---|
| 首次复现耗时过长 | 高 | 高 | 第 7 天未跑通 | 使用 MMRec；缩小数据；必要时 BM3 换 VBPR |
| Amazon 2023 处理过重 | 高 | 中 | 图片/元数据对齐反复失败 | 先用 MMRec 兼容数据，迁移放到 Gate 后 |
| 干预分布外 | 高 | 高 | 两种干预结论相反 | 加 matched replacement 与随机负对照 |
| 错配现象弱 | 中 | 高 | CI 跨零且效应小 | 转为模型诊断项目，不强写论文 |
| 简单 Prompt 即解决 | 中 | 高 | Grounded 与提示基线相同 | 强调边界发现或换题，不伪造创新 |
| LLM 成本过高 | 中 | 中 | 1k 样本超预算 | 结构化输出、缓存、只对固定样本生成 |
| 单人标注偏差 | 高 | 中 | 标签边界不稳定 | 制定规则、盲化方法名、重复标注子集 |
| 云 GPU 浪费 | 中 | 高 | 环境/路径错误 | 本地完成 smoke test 后再提交云端 |
| 结果不可复现 | 中 | 高 | seeds 方差大 | 三 seeds、manifest、环境锁定、保存 checkpoint |

---

## 14. 论文贡献的最低表述

在系统文献核验完成前，不使用“首次”。

可以尝试的保守表述：

1. 系统测量多模态推荐模型的行为模态依赖与自然语言解释模态归因之间的差异；
2. 提供覆盖图片、文本和用户历史干预的可复现审计协议；
3. 揭示图文冲突、协同依赖和历史稀疏条件下的错配规律；
4. 提出带不确定性和拒答能力的证据约束解释基线。

论文价值必须主要来自稳定发现，而不是 prompt 工程。

---

## 15. 简历交付形式

### 项目标题

`FaithRec-MM: Auditing Modality Attribution in Multimodal Recommendation Explanations`

### 简历表述模板

> 构建多模态推荐解释忠实性审计框架，复现 LightGCN/BM3，对图片、文本和交互历史实施分布内反事实干预，量化推荐模型实际模态依赖与 LLM 解释归因的错配；设计证据约束和可拒答解释生成方案，并建立可复现训练、评估与统计分析流水线。

最终需替换为真实数字：

- 数据规模；
- 模型数量；
- 错配率；
- 一致性提升；
- 不支持证据下降比例；
- 运行成本。

没有真实结果前，禁止在简历中写提升百分比。

---

## 16. 第一周每日任务

### Day 1

- 安装并阅读 MMRec；
- 跑通最小示例；
- 理解配置系统、数据格式和日志目录。

### Day 2

- 阅读 LightGCN 与 BM3 实现；
- 画出数据流；
- 固定环境文件；
- 建立 failure log。

### Day 3

- 小切分跑通 LightGCN；
- 检查训练 loss；
- 输出 Recall/NDCG。

### Day 4

- 完整切分运行 LightGCN；
- 保存 checkpoint；
- 验证加载后指标一致。

### Day 5

- 小切分跑通 BM3；
- 检查视觉/文本特征维度；
- 输出单个用户—商品分数。

### Day 6

- 完整切分 BM3 smoke test；
- 记录显存、时间和异常；
- 确定是否需要 L4。

### Day 7

- 整理复现笔记；
- 冻结第一版数据配置；
- 执行 Gate：继续 BM3，或降级到 VBPR。

---

## 17. 最终决策规则

### 项目成功

即使没有论文，只要做到：

- 两模型可复现；
- 一套干预审计；
- 一套解释评估；
- 可信结果；
- 完整开源仓库；
- 清楚说明负结果和局限；

就已经是合格且有辨识度的实习项目。

### 论文继续

必须同时满足：

- 现象稳定；
- 干预可信；
- 不是简单 prompt 技巧；
- 有跨模型或跨场景证据；
- 统计与复现闭环；
- 还有至少 3–4 周可用于写作和补实验。

### 论文停止

若核心发现不稳定，应停止“冲会”叙事。可信的负结果和完善的工程仍然有价值，强行扩展只会降低项目完成度。

---

## 18. 权威起点

- Amazon Reviews 2023：<https://amazon-reviews-2023.github.io/main.html>
- MMRec：<https://github.com/enoche/MMRec>
- BM3：<https://github.com/enoche/BM3>
- CountER：<https://arxiv.org/abs/2108.10539>
- LXR：<https://openreview.net/forum?id=VAEYN1pGX9>
- FIRE：<https://arxiv.org/abs/2508.05225>

这些资料用于启动复现和界定创新，不替代后续 30–50 篇系统文献矩阵。
