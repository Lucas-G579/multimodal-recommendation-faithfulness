# Compatibility environment for the MMRec models used by FaithRec-MM.
# Upstream pins Python 3.7 / torch 1.11 and cannot be installed on Python 3.11.
# PyTorch and torchvision are installed separately from the official cu121 index.

numpy==1.26.4
pandas==2.0.3
scipy==1.11.4
PyYAML==6.0.2
matplotlib==3.8.4
tqdm==4.66.4
lmdb==1.4.1
Pillow==10.3.0
gdown==6.0.0
