# 多模态推荐解释忠实性研究方案总结

## 一、拟定题目

### 中文题目

**多模态推荐系统是否解释了其真正使用的证据？基于反事实审计的忠实推荐解释研究**

也可以表述为：

**面向多模态推荐系统的解释忠实性评估与证据约束生成研究**

### 英文题目

**Do Multimodal Recommenders Explain What They Actually Use? Counterfactual Auditing and Evidence-Grounded Explanation Generation**

方法名称可暂定为：

**FaithRec-MM: A Counterfactual Auditing Framework for Faithful Multimodal Recommendation Explanations**

## 二、研究背景

随着推荐系统逐渐融合商品图片、文本描述、用户历史行为和协同过滤信号，多模态推荐模型能够利用更丰富的信息预测用户偏好。

与此同时，大语言模型具有较强的自然语言生成能力，因此越来越多的研究开始使用大语言模型为推荐结果生成个性化解释。例如，系统可能告诉用户：

> “推荐这件黑色夹克，是因为它的简约风格与你过去购买的服饰非常相似。”

这类解释通常具有较好的流畅性、可读性和说服力，但仍然存在一个关键问题：

> 这段解释是否真实反映了推荐模型的决策依据？

推荐模型可能实际上主要依赖用户—商品交互关系、商品流行度或协同过滤信息，而不是商品的黑色属性或简约风格。大语言模型之所以提到这些属性，可能只是因为它们更容易从商品图片和文本中识别和表达。

因此，一段看似合理的解释，未必是一段忠实的解释。

本研究关注的核心不是：

- 解释是否流畅；
- 解释是否具有说服力；
- 解释是否符合一般常识；
- 解释是否与商品描述表面相关。

本研究真正关注的是：

> 推荐解释所声称的重要证据，是否确实影响了推荐模型的预测和排序结果。

## 三、核心概念：什么是推荐解释忠实性

本研究将推荐解释的质量区分为三个层面。

### 1. 流畅性

解释在语言表达上是否自然、通顺、易于理解。

例如：

> “这件夹克与你偏爱的简约风格高度契合。”

这句话语言上可能非常流畅。

### 2. 合理性

解释是否符合用户偏好、商品属性和一般认知。

例如，用户过去浏览过多件黑色服饰，那么“用户偏爱黑色”在表面上是合理的。

### 3. 忠实性

解释是否准确反映推荐模型真正使用的决策证据。

如果把商品颜色从黑色改为红色，推荐分数几乎完全不变，那么模型实际上可能并不依赖颜色。此时解释中强调“黑色”虽然看起来合理，却并不忠实。

因此，本研究将解释忠实性定义为：

> 自然语言解释中所陈述的推荐依据，应当与推荐模型在输入干预和反事实条件下表现出的真实决策依赖保持一致。

换言之，如果解释声称某一信息是推荐原因，那么改变或移除该信息，应当引起推荐分数、排名或结果的相应变化。

## 四、基本研究场景

假设用户过去交互过以下商品：

- 黑色卫衣；
- 黑色运动鞋；
- 极简风双肩包。

推荐系统向该用户推荐一件黑色简约夹克，并生成解释：

> “你过去偏爱黑色和简约风格，因此系统为你推荐这件夹克。”

这段解释包含两个主要证据：

1. 用户偏好黑色；
2. 用户偏好简约风格。

为了验证这两个证据是否忠实，可以进行以下干预。

### 干预一：修改商品图片

将夹克图片中的黑色修改为红色，或者使用同类别、同风格但颜色不同的商品图片替换原图片。

如果推荐分数和排名基本不变，说明颜色可能并不是模型的重要决策依据。

如果解释仍然强调“黑色”，则解释明显不忠实。

### 干预二：修改商品文本

将文本中的“black minimalist jacket”改为“red minimalist jacket”，只改变颜色属性，其他信息保持不变。

如果推荐结果发生明显变化，说明模型可能确实使用了文本中的颜色信息。

### 干预三：删除用户历史

删除用户历史中的黑色卫衣和黑色双肩包。

如果目标夹克从推荐列表中消失，说明用户历史中的黑色偏好确实是推荐成立的重要条件。

### 干预四：构造图文冲突

让商品图片显示黑色夹克，但文本描述为红色夹克。

此时可以观察：

- 推荐模型更依赖图片还是文本；
- 解释生成器更依赖图片还是文本；
- 解释是否能够识别图文矛盾；
- 解释是否会自动编造一个看似一致的理由。

由此便可以检验：

> 推荐模型实际依赖的证据，与自然语言解释声称使用的证据是否一致。

## 五、研究对象与系统结构

本研究建议采用“推荐模型与解释生成器分离”的研究设计，而不是让一个大语言模型同时负责推荐和解释。

整体系统可以分为三层。

## 第一层：多模态推荐模型

推荐模型接收以下输入：

[
u,\ H_u,\ I_i,\ T_i
]

其中：

- (u)：目标用户；
- (H_u)：用户历史交互记录；
- (I_i)：候选商品图片；
- (T_i)：商品标题、描述和属性文本；
- (i)：候选商品。

推荐模型输出用户 (u) 对商品 (i) 的预测分数：

[
s_{ui}=f(u,H_u,I_i,T_i)
]

推荐模型可以包括：

- LightGCN；
- VBPR；
- MMGCN；
- BM3；
- FREEDOM；
- 其他典型多模态推荐模型。

本研究不一定需要重新设计一个复杂的推荐主模型，而是将现有推荐模型作为需要被审计和解释的对象。

## 第二层：自然语言解释生成器

解释生成器根据以下信息生成推荐理由：

- 用户历史商品；
- 当前推荐商品图片；
- 当前推荐商品文本；
- 推荐模型的输出分数；
- 用户和商品的表示向量；
- 推荐模型识别出的重要证据。

基础解释方式可以包括：

### 1. Direct Prompt

直接把用户历史、商品图片描述和文本输入大语言模型，让其自由生成推荐解释。

### 2. Caption-Based Explanation

先使用视觉语言模型将商品图片转化为图片描述，再将图片描述、商品文本和用户历史输入大语言模型。

### 3. Collaborative-Aware Explanation

在解释生成过程中加入用户—商品协同过滤信息，例如相似用户、相似商品、用户和商品嵌入等。

### 4. Evidence-Grounded Explanation

先通过反事实干预识别真正影响推荐结果的证据，再要求大语言模型只能基于这些已验证证据生成解释。

前三类可以作为基线，第四类是本研究拟提出的主要方法。

## 第三层：忠实性审计模块

忠实性审计模块用于验证解释中的每一项证据。

例如，解释中包含以下内容：

- 用户喜欢黑色；
- 用户喜欢极简风格；
- 商品采用真皮材质。

系统需要分别检验：

- 改变黑色属性是否影响推荐结果；
- 改变极简风格是否影响推荐结果；
- 改变真皮材质是否影响推荐结果。

如果改变真皮材质后，推荐分数没有明显变化，那么“真皮材质”就属于解释中缺乏模型证据支持的内容。

## 六、研究问题

本研究可以围绕四个核心研究问题展开。

## RQ1：现有大语言模型生成的推荐解释是否忠实？

该问题检验：

> 一段自然、合理、个性化的解释，是否真正反映推荐模型的决策依据。

可能出现的结果是：

- 直接生成的解释具有较高流畅性；
- 人工评价认为解释合理；
- 但解释所提到的特征被删除后，推荐结果几乎没有变化；
- 解释遗漏了真正重要的协同过滤信号；
- 解释更像是对商品和用户历史的总结，而不是对推荐模型的解释。

该问题的核心结论可能是：

> 解释质量与解释忠实性并不等价。

## RQ2：不同模态之间是否存在系统性的解释错配？

多模态推荐模型通常同时使用：

- 商品图片；
- 商品文本；
- 用户历史；
- 协同过滤关系；
- 商品流行度等结构化信息。

但大语言模型生成解释时，更容易描述图片和文本中的显性语义，例如颜色、风格、功能和使用场景。

因此可能出现：

- 模型主要依赖协同信号，解释却主要描述商品文本；
- 模型主要依赖商品图片，解释却重复商品标题；
- 模型依赖用户历史中的某一商品，解释却泛化为抽象偏好；
- 模型主要利用商品流行度，解释却将其表达为个性化偏好。

本研究将这种现象定义为：

> **模态归因错配，即 modality attribution mismatch。**

研究需要回答：

1. 推荐模型对图片、文本和协同信号的实际依赖程度是多少？
2. 解释中对不同模态证据的提及比例是多少？
3. 两者之间是否一致？
4. 哪些模型、用户和商品条件下错配更严重？

## RQ3：图文不一致时，推荐模型和解释分别跟随哪一种模态？

该问题与现有的多模态一致性研究具有直接联系。

可以构造四种实验条件：

1. 图片与文本一致；
2. 只改变图片；
3. 只改变文本；
4. 图片与文本相互矛盾。

例如：

- 图片：黑色连衣裙；
- 文本：红色连衣裙。

在这种情况下，需要分别观察：

- 推荐分数变化；
- 商品排名变化；
- 模型对图片和文本的敏感程度；
- 解释中提到的是黑色还是红色；
- 解释是否识别到图文冲突；
- 解释是否错误地将冲突信息整合为一个连贯理由。

这一部分可以揭示：

> 当多模态信息发生冲突时，解释可能为了语言连贯性而掩盖模型内部真实的不确定性或模态偏好。

## RQ4：如何生成更加忠实的多模态推荐解释？

本研究拟提出一个两阶段框架：

> 先识别模型真正使用的证据，再基于这些证据生成解释。

相比让大语言模型直接根据用户历史和商品信息自由生成解释，证据约束方法具有两个优势：

1. 减少解释中没有模型依据的内容；
2. 使解释能够更准确地对应模型的实际决策逻辑。

## 七、拟提出的方法：FaithRec-MM

FaithRec-MM 可以由三个核心模块组成。

# 模块一：多模态证据结构化

首先将图片、文本和用户历史转化为可干预、可验证的结构化概念。

## 1. 商品文本概念

从商品标题、描述和属性中提取：

- 颜色；
- 材质；
- 风格；
- 图案；
- 品牌；
- 商品类别；
- 功能；
- 使用场景；
- 适用人群。

例如：

```
{
  "color": "black",
  "style": "minimalist",
  "material": "leather",
  "occasion": "daily wear"
}
```

## 2. 商品图片概念

通过视觉语言模型或属性识别模型提取：

```
{
  "visual_color": "black",
  "visual_style": "minimalist",
  "visual_pattern": "solid",
  "visual_material": "leather-like"
}
```

## 3. 用户偏好概念

根据用户历史商品提取：

```
{
  "preferred_color": ["black", "gray"],
  "preferred_style": ["minimalist", "casual"],
  "preferred_category": ["jacket", "hoodie"]
}
```

这一过程与现有的图片描述、文本嵌入和多模态一致性计算相似，但需要进一步从整体语义相似度扩展到属性级证据。

# 模块二：反事实证据审计

对于每一个可能影响推荐结果的证据 (c)，构造对应的反事实输入：

[
x^{(-c)}
]

计算原始输入和反事实输入下的推荐分数差异：

[
\Delta_c=s_{ui}(x)-s_{ui}(x^{(-c)})
]

其中：

- (s_{ui}(x))：原始输入下的推荐分数；
- (s_{ui}(x^{(-c)}))：移除或修改证据 (c) 后的推荐分数；
- (\Delta_c)：证据 (c) 对推荐结果的实际影响。

如果：

[
\Delta_c \approx 0
]

说明该证据对模型决策影响较小。

如果：

[
\Delta_c \gg 0
]

说明该证据是模型推荐目标商品的重要依据。

## 图片反事实干预

图片可以通过以下方式进行干预。

### 1. 区域遮挡

遮挡商品主体中的某一区域，例如：

- 颜色区域；
- 图案区域；
- 品牌标志；
- 局部设计。

### 2. 真实图片替换

在同类别商品中，寻找其他属性相似但目标属性不同的商品图片。

例如：

- 同为夹克；
- 价格相近；
- 风格相近；
- 只在颜色上不同。

### 3. 属性控制编辑

通过图像编辑模型改变单一属性，例如只改变颜色。

这种方式控制能力较强，但也可能引入生成痕迹和其他非预期变化，因此更适合作为扩展实验。

## 文本反事实干预

文本干预应尽可能只改变一个目标属性。

例如：

原始文本：

> black minimalist jacket

颜色反事实：

> red minimalist jacket

风格反事实：

> black decorative jacket

理想情况下，除了目标属性，其他词语和语义内容应保持稳定。

## 用户历史反事实干预

用户历史可以通过以下方式进行干预：

- 删除与目标商品最相似的历史商品；
- 删除某一种颜色偏好的历史商品；
- 删除某一种风格偏好的历史商品；
- 替换为相反偏好的商品；
- 删除最近交互；
- 删除长期历史；
- 只保留解释中提到的历史证据。

由此可以判断：

- 推荐是否依赖长期偏好；
- 推荐是否依赖最近兴趣；
- 推荐是否依赖某个具体历史商品；
- 解释中概括出的用户偏好是否真实影响模型。

# 模块三：证据约束解释生成

完成反事实审计后，系统会得到一组经过验证的证据。

例如：

```
Supported evidence:
1. The user previously interacted with three black clothing items.
2. Replacing the black color reduces the recommendation score by 0.18.
3. Removing minimalist-style information reduces the score by 0.11.

Unsupported evidence:
1. Material has little influence on the recommendation score.
2. Brand has no measurable influence.
```

随后要求大语言模型：

- 只能使用 supported evidence；
- 不得使用 unsupported evidence；
- 明确说明证据来自图片、文本还是用户历史；
- 不得将商品一般属性描述为模型决策原因；
- 证据不足时表达不确定性；
- 避免生成无法验证的偏好判断。

最终解释可能为：

> “系统推荐这件商品，主要因为其黑色和极简风格与你过去浏览的服饰偏好一致。反事实分析表明，这两个特征都会显著影响该商品的推荐分数。”

这样生成的解释不只是“看起来合理”，而是具有模型行为证据支持。

## 八、核心评估指标

本研究需要同时评价推荐性能、语言质量和解释忠实性。

# 一、推荐性能

基础推荐性能可以使用：

- Recall@K；
- NDCG@K；
- Precision@K；
- Hit Rate@K。

目的在于确保忠实解释方法不会严重损害原有推荐效果。

# 二、语言质量

解释文本质量可以使用：

- Fluency；
- Readability；
- Relevance；
- Personalization；
- Specificity；
- Helpfulness；
- 人工评价；
- 大语言模型辅助评价。

但这些指标不能替代忠实性评价。

# 三、忠实性指标

## 1. Evidence Precision

解释中提到的证据，有多少经过反事实验证后确实影响推荐结果：

\frac{|\hat E\cap E^*|}{|\hat E|}
]

其中：

- (\hat E)：解释中提到的证据集合；
- (E^*)：经干预验证后真正影响推荐结果的证据集合。

Evidence Precision 越高，说明解释越少包含没有模型依据的内容。

## 2. Evidence Recall

模型真正使用的重要证据，有多少被解释覆盖：

\frac{|\hat E\cap E^*|}{|E^*|}
]

Evidence Recall 越高，说明解释越少遗漏模型真正使用的重要信息。

## 3. Evidence F1

综合 Evidence Precision 和 Evidence Recall：

[
F1=
\frac{2PR}{P+R}
]

## 4. Necessity

删除解释中提到的重要证据后，目标商品是否退出 Top-(K) 推荐列表。

可以定义：

[
PN@K=
P(i\notin R'*{u,K}\mid E*{\text{explained removed}})
]

如果 PN@K 较高，说明解释中的证据对推荐结果具有较强必要性。

## 5. Sufficiency

只保留解释中提到的证据后，目标商品是否仍然处于 Top-(K) 推荐列表。

[
PS@K=
P(i\in R'_{u,K}\mid \text{only explained evidence retained})
]

如果 PS@K 较高，说明解释中的证据能够较充分地支持推荐结果。

## 6. Counterfactual Explanation Consistency

当真正重要的证据发生变化时，解释是否做出正确变化。

例如，将商品颜色从黑色变为红色：

- 推荐结果发生变化；
- 解释也相应改变颜色描述。

可以定义：

[
CEC=
P(\text{explanation changes correctly}
\mid \text{causal evidence changes})
]

需要区分以下情况：

1. 证据变化，推荐变化，解释正确变化：忠实；
2. 证据变化，推荐变化，解释不变化：不忠实；
3. 证据变化，推荐不变化，解释却变化：解释过度依赖表面内容；
4. 证据变化，推荐和解释均不变化：该证据可能不重要。

## 7. Modality Attribution Agreement

首先计算推荐模型对不同模态的实际依赖：

[a_{image},a_{text},a_{history},a_{collaborative}]
]

再计算解释中不同模态证据的提及程度：

[\hat a_{image},\hat a_{text},\hat a_{history},\hat a_{collaborative}]
]

两者可以通过以下指标比较：

- Cosine Similarity；
- Spearman Correlation；
- KL Divergence；
- Rank Correlation。

该指标用于衡量：

> 解释声称模型使用的模态，与模型实际依赖的模态是否一致。

## 九、实验设计

## 实验一：现有解释方法的忠实性诊断

比较以下解释方法：

- Direct Prompt；
- Caption-Based Prompt；
- Collaborative-Aware Prompt；
- 基于用户评论的解释；
- Evidence-Grounded Prompt。

评价：

- 流畅性；
- 个性化；
- Evidence Precision；
- Evidence Recall；
- Necessity；
- Sufficiency；
- Counterfactual Consistency。

核心目标是验证：

> 传统方法虽然生成质量较高，但反事实忠实性可能较低。

## 实验二：不同推荐模型的解释忠实性比较

选取：

- 一个协同过滤模型；
- 一个视觉推荐模型；
- 两至三个多模态推荐模型。

观察：

- 不同模型是否具有不同的模态依赖；
- 解释生成器是否能够识别这些差异；
- 模型复杂度是否会增加解释错配；
- 推荐准确率更高的模型是否一定更容易解释。

## 实验三：图文冲突实验

构造：

- 图文一致样本；
- 图片改变样本；
- 文本改变样本；
- 图文冲突样本。

检验：

- 模型到底跟随图片还是文本；
- 解释到底跟随图片还是文本；
- 模型依赖与解释依赖是否一致；
- Evidence-Grounded 方法能否降低冲突条件下的解释错误。

## 实验四：用户和商品异质性分析

分析忠实性在不同条件下的差异。

### 用户层面

- 活跃用户与低活跃用户；
- 历史丰富用户与冷启动用户；
- 偏好集中用户与偏好多样用户；
- 长期稳定用户与兴趣漂移用户。

### 商品层面

- 热门商品与长尾商品；
- 图文一致商品与图文冲突商品；
- 文本丰富商品与文本稀疏商品；
- 图片信息丰富商品与图片信息弱商品；
- 新商品与成熟商品。

可能的研究发现包括：

- 冷启动用户的解释更依赖商品表面特征；
- 热门商品的推荐原因容易被错误解释为用户偏好；
- 图文冲突越强，解释的模态归因错配越严重；
- 用户历史越稀疏，大语言模型越容易生成泛化或模板化解释。

## 实验五：方法有效性和消融实验

可以设置以下消融版本：

- 去除图片证据审计；
- 去除文本证据审计；
- 去除用户历史证据审计；
- 去除必要性约束；
- 去除充分性约束；
- 不区分证据来源；
- 不加入不确定性表达；
- 不进行图文冲突识别。

检验各模块对忠实性的具体贡献。

## 十、数据集选择

## 首选数据集：Amazon Reviews 2023

推荐选择 Amazon Reviews 2023 中的服装或相关类别。

该数据能够提供：

- 用户—商品交互；
- 用户评论；
- 商品标题；
- 商品描述；
- 商品元数据；
- 商品图片；
- 商品类别；
- 时间信息。

### 推荐类别

首选：

- Clothing；
- Shoes；
- Beauty；
- Sports and Outdoors；
- Pet Supplies。

其中服装类别最适合进行属性级干预，因为图片和文本中包含清晰的：

- 颜色；
- 图案；
- 风格；
- 材质；
- 长短；
- 款式；
- 使用场景。

这些属性既容易识别，也容易构造反事实样本。

## 现有 Decentraland 数据的使用方式

现有 Decentraland 数据可以复用以下技术积累：

- 商品图片预处理；
- 图像 caption 生成；
- 图像和文本 embedding；
- 图文一致性计算；
- 商品视觉特征提取；
- 商品语义特征构建；
- 多模态变量设计。

但如果现有数据缺少完整的用户—商品交互历史，则不适合作为主推荐数据集。

Decentraland 数据更适合作为：

- 外部有效性案例；
- 虚拟商品场景扩展；
- 冷启动商品分析；
- 图文冲突和商品属性实验；
- 管理学意义上的补充研究。

## 十一、预期理论与方法贡献

本研究可以形成五方面贡献。

### 贡献一：重新界定多模态推荐解释质量

现有研究往往强调解释的：

- 流畅性；
- 可读性；
- 相关性；
- 个性化；
- 说服力。

本研究进一步强调：

> 推荐解释必须与推荐模型的实际决策机制保持一致。

### 贡献二：提出多模态推荐解释的反事实审计框架

分别对：

- 商品图片；
- 商品文本；
- 用户历史；
- 协同信号。

进行反事实干预，识别各类证据对推荐结果的真实影响。

### 贡献三：揭示模态归因错配现象

系统分析：

> 推荐模型真正依赖的模态，与自然语言解释声称使用的模态之间存在何种差异。

尤其关注：

- 图片—文本错配；
- 语义信息—协同信息错配；
- 用户历史—商品属性错配；
- 热门度—个性化偏好错配。

### 贡献四：研究图文冲突下的解释忠实性

将多模态一致性问题引入推荐解释研究，分析图文冲突条件下：

- 推荐模型的模态偏好；
- 解释生成器的模态偏好；
- 解释对冲突的处理方式；
- 模型决策和解释内容之间的一致性。

### 贡献五：提出证据约束的解释生成方法

通过“证据识别—反事实验证—约束生成”的过程，使自然语言解释只包含经过模型行为验证的证据。

该方法不一定需要训练大型模型，可以采用：

- Prompt 约束；
- 检索增强；
- 轻量适配器；
- LoRA；
- 重排序器；
- 小型证据选择网络。

因此具有较好的可实现性和可复现性。

## 十二、最可能形成的论文故事

这篇论文不应只讲：

> 我们提出了一个方法，使某个指标提高了若干百分点。

更完整的论文故事应该是：

### 第一部分：发现问题

现有大语言模型生成的多模态推荐解释虽然流畅、合理且个性化，但无法稳定通过反事实忠实性检验。

### 第二部分：解释问题来源

解释生成器倾向于使用容易语言化的图片和文本语义，却忽略推荐模型真正依赖的协同关系、历史交互或流行度信息，由此产生模态归因错配。

### 第三部分：识别问题发生条件

在以下条件下，解释错配可能更加严重：

- 图文不一致；
- 用户历史稀疏；
- 商品处于长尾；
- 推荐模型高度依赖协同信号；
- 用户偏好发生漂移；
- 商品图片和文本贡献接近；
- 商品流行度较高。

### 第四部分：提出解决方案

通过跨模态反事实审计识别真实推荐证据，并使用证据约束方式生成自然语言解释。

### 第五部分：验证效果

新方法能够在保持推荐性能和语言可读性的基础上，显著提升：

- Evidence Precision；
- Evidence Recall；
- Necessity；
- Sufficiency；
- Counterfactual Consistency；
- Modality Attribution Agreement。

最终形成：

> 新问题定义＋系统性诊断＋反事实评估框架＋轻量解决方法。

## 十三、与现有研究基础的关系

本研究可以自然承接已有的两个研究方向。

### 与多模态一致性研究的关系

已有研究关注：

> 商品图片与文本是否一致，以及一致性如何影响收藏、购买和销售表现。

本研究进一步关注：

> 当图片和文本共同进入推荐模型时，模型是否真正使用了这些信息，生成的推荐解释是否忠实反映这种使用。

两者关系为：

- 原研究：多模态一致性对用户行为和市场表现的影响；
- 新研究：多模态信息对算法决策和解释忠实性的影响。

### 与跨领域推荐研究的关系

已有研究涉及：

- LLM embedding；
- 跨领域推荐；
- 语义稀疏；
- 冷启动；
- MoE；
- VAE；
- 对比学习。

新研究可以进一步关注：

- 大语言模型在推荐解释中的作用；
- 协同信号如何传递给解释生成器；
- LLM 是否会忽略推荐模型的真实依据；
- 冷启动和数据稀疏是否导致解释不忠实；
- 如何使用轻量方法提高解释与推荐模型之间的一致性。

因此，新题目并不是完全重新开始，而是在现有推荐系统和多模态研究积累上的自然延伸。

## 十四、最小可行研究版本

为了保证个人能够完成，第一阶段不应设计得过于复杂。

最小可行版本可以包括：

### 数据

- Amazon Clothing 一个类别。

### 推荐模型

- LightGCN；
- 一个典型多模态推荐模型，如 BM3 或 FREEDOM。

### 解释模型

- 一个开源大语言模型；
- 一个视觉语言模型用于图片概念提取。

### 解释方法

- Direct Prompt；
- Caption-Based Prompt；
- Evidence-Grounded Prompt。

### 干预类型

- 图片干预；
- 文本干预；
- 用户历史删除。

### 忠实性指标

- Evidence Precision；
- Evidence Recall；
- PN@K；
- PS@K；
- Counterfactual Explanation Consistency。

第一阶段只需要回答：

> 直接使用大语言模型生成的多模态推荐解释，能否通过反事实忠实性检验？

如果结果表明：

- Direct Prompt 的解释流畅度较高；
- 但 Evidence Precision 和 Necessity 较低；
- Evidence-Grounded 方法显著提升忠实性；

则说明题目具备继续扩展为完整论文的价值。

## 十五、主要风险与应对方案

### 风险一：解释对象不清晰

必须明确解释的是：

> 推荐模型为什么将商品 (i) 推荐给用户 (u)。

不能混淆为：

- 用户为什么喜欢商品；
- 商品为什么受欢迎；
- 大语言模型为什么生成某句话；
- 商品本身有哪些特点。

### 风险二：反事实干预不够干净

如果修改颜色时同时改变类别、风格和背景，则无法判断推荐结果变化究竟由什么因素造成。

应尽可能遵循：

> 一次只改变一个目标属性，其他信息保持稳定。

### 风险三：过度依赖大语言模型评价

不能仅仅让另一个大语言模型判断解释是否忠实，因为评价模型无法直接知道推荐模型实际依赖了什么信息。

大语言模型可以评价：

- 流畅性；
- 可读性；
- 相关性；
- 具体性。

但忠实性必须主要通过：

- 输入干预；
- 推荐分数变化；
- 排名变化；
- 必要性；
- 充分性；
- 模态归因一致性。

### 风险四：创新退化为“特征重要性＋LLM”

仅仅使用 SHAP 或特征重要性，然后让大语言模型写一段解释，创新性可能不足。

需要突出以下三个核心创新：

1. 多模态推荐中的模态归因错配；
2. 图文冲突条件下的解释忠实性；
3. 图片、文本和用户历史的跨模态反事实审计。

### 风险五：图片干预成本较高

图片属性编辑可能需要大量模型调用和人工检查。

可以采用分阶段方案：

1. 首先使用同类真实商品图片替换；
2. 再进行区域遮挡；
3. 最后将生成式属性编辑作为补充实验。

## 十六、阶段性研究路线

### 第一阶段：问题验证

目标是确认现有解释确实存在忠实性问题。

完成：

- 数据集处理；
- 推荐模型复现；
- Direct Prompt 解释生成；
- 基础反事实干预；
- 第一组忠实性结果。

### 第二阶段：系统诊断

分析：

- 不同模态；
- 不同推荐模型；
- 不同用户；
- 不同商品；
- 图文冲突条件。

确定解释错配的主要来源。

### 第三阶段：方法设计

实现：

- 多模态证据提取；
- 反事实证据评分；
- 证据筛选；
- 证据约束生成。

### 第四阶段：完整实验

完成：

- 主实验；
- 消融实验；
- 异质性分析；
- 鲁棒性实验；
- 人工评价；
- 成本和效率分析。

### 第五阶段：论文写作

最终形成：

- 问题定义；
- 相关工作；
- 评估框架；
- 方法；
- 实验；
- 讨论；
- 局限性与未来研究。

## 十七、最终定位

本研究的核心并不是让推荐解释“写得更漂亮”，而是回答一个更加基础和重要的问题：

> 当多模态推荐系统向用户说明推荐理由时，它是在真实揭示模型的决策依据，还是仅仅根据商品内容生成了一段听起来合理的故事？

因此，研究的核心逻辑可以概括为：

1. 多模态推荐模型使用图片、文本、用户历史和协同信号进行决策；
2. 大语言模型根据这些信息生成自然语言解释；
3. 自然语言解释可能与推荐模型的真实决策机制不一致；
4. 通过反事实干预可以验证解释中的证据是否真实影响推荐结果；
5. 在此基础上，可以识别模态归因错配和图文冲突问题；
6. 最终通过证据约束生成更加忠实的推荐解释。

一句话概括该研究：

> **通过跨模态反事实干预审计推荐模型真正使用的图片、文本和用户历史证据，并基于这些证据生成忠实、可验证的自然语言推荐解释。**