name: faithrec-mm
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # Install PyTorch separately from the official CUDA wheel index.
      - -r requirements-mmrec-py311.txt
